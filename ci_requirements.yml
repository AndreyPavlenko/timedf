channels:
  - conda-forge
  - intel
  - default
dependencies:
  - pytest-html
  - braceexpand
  - s3fs
  - mysql
  - mysql-connector-python
  - numpy>=1.11
  - pandas>=1.0.0
  - conda
  - scikit-learn-intelex
  - pip
  - psutil
  - black=19.10b0
  - flake8
  - Cython
  - pyarrow<=5.0
  - arrow-cpp<=5.0
  - cmake>=3.15
  # pip with old git failed build asv from master; conflict with PEP440
  - git>=2.0.0
  # benchmarks need one tbb component for performance improvement
  - tbb
  - pip:
      - xgboost >=1.3
